import org.junit.Test;

import static org.junit.Assert.*;

public class swapAndPrintTest {
    @Test
        public void swapAndPrint() {
        swapAndPrint swap = new swapAndPrint();
        swap.swapAndPrint(10,20);
    }
}